import Foundation

/// The symbol for a book's authors, shared by the book screen's metadata
/// lines and the group views.
public let authorIconName = "person.fill"

/// The symbol for a book's narrators, shared by the book screen's metadata
/// lines and the group views.
public let narratorIconName = "mic.fill"

/// The symbol for a book's genres, shared by the book screen's metadata
/// lines and the group views.
public let genreIconName = "tag.fill"

/// The symbol for a book's length, shared by the book screen's metadata
/// lines and the sort menu.
public let durationIconName = "clock.fill"

/// The symbol for a book's year, shared by the book screen's metadata lines
/// and the sort menu.
public let yearIconName = "calendar"
